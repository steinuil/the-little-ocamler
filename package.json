{
  "name": "the-little-ocamler",
  "version": "1.0.0",
  "description": "the little ocamler code from book",
  "main": "index.js",
  "scripts": {
    "start": "bsb -make-world -w",
    "build": "bsb -make-world",
    "clean": "bsb -clean-world",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "devDependencies": {
    "bs-platform": "^7.3.0"
  }
}
